import { SexReformPipe } from './sex-reform.pipe';

describe('SexReformPipe', () => {
  it('create an instance', () => {
    const pipe = new SexReformPipe();
    expect(pipe).toBeTruthy();
  });
});
